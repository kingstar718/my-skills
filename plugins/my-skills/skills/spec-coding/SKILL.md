---
name: spec-coding
description: "按 Spec Coding（规范驱动开发）跟进需求：每个需求一份单文件 spec（状态/需求与验收标准/设计要点/任务清单/测试先行/回归清单），先在 md 登记并定稿，再测试先行（红→绿）实现，回归通过后更新状态。用于新需求登记、需求文档编写与维护、验收标准与回归清单管理。"
---

# 需求跟进（Spec Coding，单文件 spec）

原则：**一份 spec 一个 md，是唯一真相源；先在 md 登记并定稿，再实现，最后回归。**

## 流程

1. 登记：新需求在 `docs/` 建 `xxx需求.md`（单文件模板见 [assets/spec-template.md](assets/spec-template.md)，完整示例见 [assets/spec-example.md](assets/spec-example.md)），同时在 `docs/project.md` 需求登记表加一行，状态 `规划中`。
2. 定稿：spec 中 `待确认项` 全部确认后，状态转 `开发中`，之后才允许动代码。
3. 测试先行：按 `需求与验收标准` 逐条写单测，现有实现下先跑红，再实现到绿。
4. 回归：把单测/打包/真实环境结果写进 `回归清单`；真实环境通过后状态转 `已上线`。
5. 收尾：更新文档索引（如 AGENTS.md）；commit message 引用 spec 文件名。

## 单文件 spec 结构（顺序即阅读顺序）

1. **头部状态行**：`规划中 / 开发中 / 待回归 / 已上线`，状态只维护这一处；需求登记表只放一行摘要。
2. **需求与验收标准**：用 EARS 句式（`WHEN 触发条件，系统 SHALL 行为；IF 边界条件，THEN 结果`）写，每条编号 `AC-1/AC-2…`，可直接复制成测试用例；任务和用例引用编号保证可追溯；入参约束（格式、数量上限、长度上限、空值处理）写在这里。
3. **设计要点**：改动点（类/方法/接口）、关键决策与原因、影响范围（接口/配置/依赖/数据库/外部服务）。
4. **任务清单**：原子任务表（依赖、关联验收标准、验证方式、状态），按依赖排序。
5. **待确认项**：评审后删除或转成结论；全部确认后才转 `开发中`。
6. **测试先行（TDD）**：红→绿对照表，用例与 AC 编号一一对应。
7. **回归清单**：用例/预期/实际。

变更历史不单独维护：需求变更 = 先改本 spec 再改代码，git 历史即修订记录。

## 约束

- 小改动（一行需求、缺陷修复）不建独立 spec：直接在 `docs/project.md` 登记表加一行并写清验收与回归结果；涉及接口契约变更才补 spec。
- 验收标准必须可测且编号：`AC-1/AC-2…`，单测用例与验收标准一一对应，宁可少而准。
- 入参约束要明确：格式、数量上限、长度上限（防穷举/防超长）。
- 需求变更 = 先改 spec 再改代码，并补回归用例；commit message 引用 spec 文件名。
- 构建环境、工具链等约定不进 spec，放 AGENTS.md。
- 文件命名：`docs/xxx需求.md` 或 `xxx接口文档.md`，一个需求一份。
